import MotionHoc from "./MotionHoc";

const ProfileComponent = () => {
  return <h1 className="headerbox">Company Profile</h1>;
};

const Profile = MotionHoc(ProfileComponent);

export default Profile;
